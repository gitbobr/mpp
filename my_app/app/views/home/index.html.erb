<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">
      <img src="https://cdn.icon-icons.com/icons2/3653/PNG/512/furniture_shop_chair_icon_228254.png" alt="Logo" width="30" height="24" class="d-inline-block align-text-top">
      FShop
    </a>
	<div class='d-flex justify-content-end'>
      <% if user_signed_in? %> 
	  Logged in as <%= current_user.email %> ᅠ ᅠ
      <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-sm btn-outline-secondary"  %>
      <% else %>
      <%= button_to "Sign up", new_user_registration_path, method: :get, class: "btn btn-sm btn-outline-secondary" %>
	  <%= button_to "Log in", new_user_session_path, class: "btn btn-sm btn-outline-secondary" %> 
      <% end %>
    </div>
  </div>
</nav>
<h1>Main page of furniture shop</h1>

